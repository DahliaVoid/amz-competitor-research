---
name: amz-competitor-research
description: 从商品维度 CSV（卖家精灵 / Seller Sprite 导出）出发，产出可追溯的 Amazon 竞品调研报告（单文件 HTML）。
whenToUse: 需要评估 Amazon 类目机会、分析竞品格局与价格带、判断新品切入方向、或从标题语料提取卖点时使用。
disable-model-invocation: true
user-invocable: false
---

# SKILL: Amazon 竞品调研（amz-competitor-research）

从**商品维度 CSV**（卖家精灵 / Seller Sprite 导出）出发，产出**可追溯的竞品调研报告（HTML）**。

本 skill 负责**流程**：输入契约 → 事实层 → 趋势层 → 推理层 → HTML 交付。
证据口径（观察/推断/建议的标注、分母、覆盖范围、缺失值处理）遵循 `amazon-ops-playbook`；两者定义冲突时以本 skill 的字段定义为准，并在报告中注明所用定义。

适用：类目机会评估、竞品格局与价格带分析、新品切入判断、标题/卖点语料提取。
不适用：广告投放优化、Listing 改写执行、供应链与利润测算（需要 Seller Central / Ads 控制台导出，本 skill 不持有这些数据）。

---

## 1. 输出约定

1. **交付物是单文件 HTML**，由 `scripts/gen-html.js` 生成：内联 CSS + 内联 SVG 图表，不引用外部资源，除 Node 外不需要任何运行时。
2. `analyze.js` **只写事实**（facts.json / facts.md），**从不写 `*_report.md`**。报告正文由 Agent 撰写，两者命名刻意区分，避免把脚本摘要当成调研报告交付。
3. 任何进入报告的数字，必须能追溯到：CSV 的具体列、`facts.json` 的具体字段、或 `trend_context.json` 中带来源的条目。做不到就写 `不可用` 并说明需要哪份导出。
4. **报告页眉只写标题 + 一行元信息**（生成时间 / 数据来源 / 样本量 / 时间范围）。脚本文件名、路径、渲染时间、字段口径一律放附录，不得出现在页眉。

---

## 2. 输入契约

### 2.1 CSV 要求

| 项 | 要求 |
| --- | --- |
| 必备列 | `ASIN`（脚本靠它定位表头；没有则报错退出） |
| 核心列 | `品牌`、`商品标题`、`价格`、`月销量`、`月销售额`（列名可带货币单位，如 `月销售额($)`） |
| 常用列 | `上架天数`、`上架时间`、`小类目`、`类目路径`、`小类BSR`、`评分数`、`月新增评分数`、`评分`、`配送方式`、`卖家数`、`变体数`、`LQS`、`Amazon's Choice`、`Best Seller标识`、`New Release标识`、`A+页面`、`视频介绍`、`SP广告`、`Coupon`、`月销量增长率` |
| 表格噪音 | 导出文件头部的说明行、尾部的签名/地址块会被自动跳过并计数，不需预处理 |
| 多行单元格 | 带引号的多行文本（如「小类目」含两行）按 CSV 规范解析为单个单元格 |

### 2.2 列可用性（决定指标能不能算）

脚本区分三种状态，**绝不混为一谈**：

| 状态 | 含义 | 该列相关指标 |
| --- | --- | --- |
| `ok` | 列存在且有数据 | 正常计算 |
| `empty-column` | 列存在但整列为空（导出未填充） | 输出 `n/a`；徽章类字段**不得**解读为「0% 占比」 |
| `missing-column` | CSV 中找不到该列 | 输出 `n/a`，并在 facts.md 与 facts.json 中列出 |

> `n/a` 表示「分母无效或源列不可用」，**不等于 0**。报告里不得把 `n/a` 写成 0、`-` 或空白。

### 2.3 多站点 / 多币种

- 货币符号**只从表头括号内识别**（`月销售额($)` → `$`），不根据站点或文件名猜测；`--currency` 可强制覆盖。
- 列名匹配会忽略大小写、空格、括号与货币符号，因此 `月销售额(¥)`、`月销售额(€)` 会自动映射到同一字段。
- 货币符号为 `¥` 时无法区分 JPY / CNY，脚本会给出提示，报告必须结合站点确认后再解读金额。
- 非卖家精灵导出、或列名完全不同时，用 `--map` 提供别名文件，不要修改脚本内置表：

```json
{ "revenue": ["Umsatz", "売上"], "sales": ["Stückzahl"], "days": ["Tage online"] }
```

---

## 3. 流程

### 3.1 事实层 —— `scripts/analyze.js`

```bash
node scripts/analyze.js <csv路径> [选项]
```

| 选项 | 默认 | 说明 |
| --- | --- | --- |
| `--out <dir>` | `scripts/` 的上一级 | 输出根目录（`data/`、`reports/` 建在其下） |
| `--map <file.json>` | — | 追加列名别名 |
| `--currency <text>` | 表头识别 | 强制货币标注 |
| `--new-days <n>` | 180 | 新品窗口天数 |
| `--top-brands / --top-products / --top-keywords <n>` | 10 / 20 / 15 | 输出行数 |
| `--keyword-min-share <f>` | 0.02 | 关键词最低出现比例 |
| `--stopwords <file>` | 内置英文表 | 追加停用词（每行一个，`#` 为注释） |
| `--max-rows <n>` | 2000 | `facts.skuTable` 内嵌行数上限 |
| `--title-terms <n>` | 25 | 每个语料保留的词/短语条数（想深挖卖点就调大） |
| `--emerging-min <n>` | 2 | 进入「新品池高倍率短语」所需的新品池出现次数 |
| `--quiet` | — | 不打印摘要 |

输出：

- `data/<csv名>_<UTC时间戳>_facts.json` —— 机器可读的完整事实层
- `reports/<csv名>_<UTC时间戳>_facts.md` —— 同内容的人读摘要（**不是**调研报告）
- `reports/latest_facts.md` —— 最新一次的副本

`facts.json` 中可直接引用的关键块：

| 块 | 内容 |
| --- | --- |
| `source` | 文件名、解析/保留/跳过行数、**columnsUsed**、**availability**（每个字段的列名、状态、非空行数）、`fieldsMissing`、`fieldsEmpty`、货币与来源 |
| `market` | SKU 数、有价格 SKU 数、月销售额、月销量、算术平均标价（分母＝有价格 SKU 数）、标价中位数、件均销售额（分母＝月销量合计）、月销量为 0 的 SKU 数 |
| `priceBands` | 价格带 × SKU 数 / 销量 / 销售额 / 单 SKU 平均销售额 / 销售额中位数 / **代表品牌 `topBrands`** |
| `listingAge` | 上架时长分档 × 同上 |
| `brandShare` + `riskSignals.concentration` | TOP 品牌表（含 `priceMin/priceMax/priceMedian`、`medianDays`）+ CR3/CR5/CR10 + TOP-N 商品销售额占比（分母＝样本月销售额） |
| `keywordFreq` + `keywordStats` | 标题高频词（全样本）+ 分母/阈值/停用词来源 |
| `titleAnalysis` | **标题语料分层结果**：按 `all / top / new` 三个语料分别给出 `words`（单词）、`phrases`（2–3 词短语）、`quantified`（含数字的参数短语，如 `6 modes`、`3 heat`）；每项带 `count/n/pct`。另含 `emerging`：新品池相对全样本的**高倍率短语**（带 lift），用于提炼新卖点方向 |
| `topProducts` / `recentProducts` / `recentPool` | 头部商品、新品池、新品池中位数（销量/销售额/评分数/标价/上架天数） |
| `riskSignals.barriers` | 评分数中位数（全样本 vs TOP-N）、评分中位数、各类徽章占比（不可用时为 `available:false`） |
| `riskSignals.notComputableFromSnapshot` | **单期快照算不出来的验证项清单**，必须原样进入报告的缺口部分 |
| `skuTable` | 规范化行数据（按 CSV 原始行序，未排序），含单品分析所需字段：价格、月销量、月销售额、上架天数与上架时间、评分与评分数、月新增评分数、留评率、**毛利率 `grossMargin`**、**FBA 费 `fbaFee`**、**尺寸分段 `sizeTier`**、卖家数、变体数、LQS、徽章与内容项、小类目 |
| `caveats` | 脚本级口径提示，报告必须全部承接 |

**两个价格口径不可混用**：

- `avgListPrice` 算术平均标价 —— 分母是**有价格的 SKU 数**，与销量无关；
- `revenuePerUnit` 件均销售额 —— 分母是**月销量合计**，是销量加权结果。

引用时必须写清「哪个口径 + 分母是多少」。同一份样本里两者可以差 20% 以上，混用会直接导致定价结论反向。

### 3.2 趋势层 —— Agent 检索（脚本不做）

要求：

1. 至少 2 类来源，优先一手资料（Seller Central / Ads 控制台导出、Amazon 官方文档、品牌自有页面）；第三方估算（Google Trends 指数、Keepa 历史、行业报告）必须标注为估算并给出处。
2. 每条来源记录 **URL + 读取日期**；读取日期是页面数据的时点，必须写进报告。
3. 检索不到就留空并写明缺哪一项，**不得用常识或类推补数**。

统一写入 `data/<csv名>_<时间戳>_trend_context.json`：

```json
{
  "metric": "月销量指数",
  "unit": "index",
  "currency": null,
  "series": [
    { "label": "2024-05", "value": 100 },
    { "label": "2025-05", "value": 112 },
    { "label": "2026-05E", "value": 120, "projected": true }
  ],
  "sources": [
    { "name": "Google Trends", "url": "https://trends.google.com/...", "readOn": "2026-05-14" }
  ],
  "notes": "检索范围、口径差异、已知偏差"
}
```

- 渲染器同样接受 `yoy` / `historical` / `history` + `projection` / `forecast` 的等价写法；带 `projected: true` 的点在图上标注「预测」。

可选补充块（渲染器忽略，供报告引用；每一项都要能指回某个来源）：

```json
{
  "calendar": [{ "name": "Prime Day 2026", "window": "2026-06-23 ~ 2026-06-26", "source": "https://..." }],
  "gaps": ["Google Trends 原始指数未取（需手动导出 CSV）"],
  "notes": "口径差异与已知偏差"
}
```

- 没有该文件时，报告与 HTML 会**明确显示趋势图缺失**，不会画占位图。这是允许的结果：宁可缺图，不要假图。

### 3.3 推理层 —— Agent 写 `report.md`

用 `facts.json` + `trend_context.json` + 标题语料写报告。

**顺序固定为「数据 → 分析 → 结论」：先摆数据，再给判断，结论集中在末尾。发现与结论不得出现在数据之前。**

页眉只有两样东西：报告标题，以及紧随其后的一行元信息列表（`- 生成时间` / `- 数据来源` / `- 样本量` / `- 时间范围`）。**页眉不写文件名、路径、渲染时间或口径说明。**

| # | 章节 | 必含内容 |
| --- | --- | --- |
| 一 | 市场总览 | 表格：样本 SKU 数 / 月总销售额 / 月总销量 / 件均销售额（带分母）/ 算术平均标价 / 标价中位数 / TOP-N 集中度；随后给规模判断【推断】 |
| 二 | 品牌竞争格局 | TOP-N 品牌表（排名 / 品牌 / 月销售额 / 月销量 / SKU 数 / 价格区间与中位 / 中位上架天数）+ 逐个品牌一句定位【推断】 |
| 三 | 价格带分析 | 价格带表（含**代表品牌**与销售额中位数）+ 逐带机会判断；`n < 30` 的带必须注明样本不足 |
| 四 | 头部竞品单品分析 | **必须输出一张表**（3–8 行）：ASIN / 品牌 / 价格 / 月销量 / 月销售额 / 上架天数 / 评分（评论数）/ 毛利率 / FBA 费 / 尺寸分段 / 徽章与内容项；表下再用列表逐条给**定位标签**与成功因素【推断】。不要用散文段落罗列单品 |
| 五 | 新品池分析（上架 ≤N 天） | 概况（n、占比、中位数）+ **新品逐个或 TOP-N 表**（含「核心卖点」列，由标题解读）+ 新品趋势（用 `titleAnalysis.emerging` 的倍率） |
| 六 | 标题语料与关键词分析 | 按语料分组的功能词 / 短语 / 量化参数词，**每项带 x/N**；再按「基础功能 / 覆盖范围 / 专业定位 / 资质 / 人群礼品 / 空白位」归类并提炼卖点；末尾给标题策略启示 |
| 七 | 市场趋势与预测 | 行业规模与 CAGR（表格带来源与读取日）+ 季节性窗口（官方大促日期）+ 12 / 24 / 36 个月展望；**没有来源支撑的年份不写数字** |
| 八 | 新品开发与运营建议 | 产品策略 / 标题策略 / 广告关键词方向 / 主图与 A+ 方向 / 风险提示 |
| 九 | 总结 | 市场吸引力 / 竞争强度 / 突破路径 / 推荐策略 / 待补验证 |
| 附录 | **置底** | A 数据台账 → B 口径与定义 → C 字段可用性 → D 数据质量提示 → E 文件清单 |

注：

- **数据台账与来源口径一律放附录**，不占正文篇幅。
- 顺序与 `amazon-ops-playbook` 默认的「范围在前」不同：本 skill 要求先数据后结论、台账置底；两种顺序包含的要素相同，报告需在附录中注明所用顺序。
- 单品分析中的「定位标签」（类目第一 / 价格杀手 / 新品黑马…）是**分析结论**，不是数据字段——必须由表中的数字支撑。
- **多行结构要用表格或列表承载**：正文段落内的单换行会被渲染成换行符（本次采用的规则，与 CommonMark 不同），但成组数据一律用表格，不要靠换行排版。

写作规则：

- 【观察】只写数据里读到的值+单位+来源；【推断】必须说明依赖哪条观察与哪个假设；【建议】必须带风险与验证方式。
- 所有比率写分母；小样本（见 4.3）不得写成类目规律。
- 变化不等于原因：只列出数据支持的候选原因，标明哪些无法用现有数据检验，以及需要什么测量才能区分。
- 标题语料只做**归纳**（高频词、参数写法、人群/场景词），不要把它当成搜索量或销量证据。

### 3.4 渲染层 —— `scripts/gen-html.js`

```bash
node scripts/gen-html.js <report.md> [输出.html] [--facts <facts.json>] [--trend <trend_context.json>] [--title <text>] [--out <dir>] [--no-charts]
```

- 默认按报告文件名中的 `<产品>_<时间戳>` 匹配同批 `facts.json` / `trend_context.json` / `chart_notes.json`；找不到则取 `data/` 下最新的。
- 输出 `<产品>_<时间戳>_report.html`，并复制一份 `reports/latest_report.html`。
- **页眉只渲染标题**，以及报告 H1 之后自带的那一行元信息列表；渲染器不注入文件名、路径、渲染时间或口径说明。
- 附图 5 张：价格带销售额、品牌销售额份额、上架时长 × 月销售额、标题关键词频次、市场趋势（需 trend_context.json）。
- 图注只写「数据来源 + 指标 + n」，**不出现内部文件名**；序列缺失时显示缺失原因，不画占位图。
- **每张图的「洞察」由 Agent 撰写**，写入 `<产品>_<ts>_chart_notes.json`，键为 `priceBand` / `brandShare` / `listingAge` / `keyword` / `trend`，或用 `--insights` 指定；未提供时该图不显示洞察行（渲染器不会自己编结论）。

---

## 4. 硬规则

### 4.1 标注与追溯
每条实质结论标注【观察】/【推断】/【建议】；每个数字可回溯到列名、facts 字段或带 URL+读取日期的来源。

### 4.2 口径与单位
保留数据原始单位，不静默换算、不年化、不改基期；金额必须带货币；比率必须带分母。

### 4.3 小样本
`n < 5` 标记为小样本（工具约定，不是数据属性）。小样本池只作为**线索**，不得作为类目规律；报告中出现小样本时必须写明 n。

### 4.4 快照 vs 多期
单期 CSV 只描述导出时点的状态。凡需要多期数据的判断（YOY、季节性、份额趋势、淘汰率、评论增速），一律在报告中列为未验证并说明所需数据源。

### 4.5 字段陷阱
- **`小类BSR` 跨行不可比**：同一导出可能混入不同小类目（「小类目」列存在多值行），BSR 量级会差几个数量级。按小类目分层后再比较，或直接不使用。
- **徽章列整列为空** ≠ 0%：说明导出未填充该字段。
- **数值列空白 = 未知（null）**，不是 0：求和时按 0 计（合计因此是**下限**），取中位数/均值时剔除。各指标的有效 n 可能小于 SKU 数，引用时必须带 n。
- **尺寸分段与 FBA 费要一起看**：「包装尺寸分段」为「小号大件」的 SKU，FBA 费可比同价位「大号标准尺寸」高 50% 以上，直接压缩毛利。定价前必须计入。
- **月销量 / 月销售额**是导出工具口径的估算值，不是 Amazon 后台成交数据；估算方法以工具官方说明为准，本 skill 不做假设，也不表述为「实际销量」。

### 4.6 禁止事项
- 不编造 ASIN、品牌、关键词、竞品名、类目均值或基准值。
- 不在脚本里写死行业结论、功能标签、季节性结论或未来预测。
- 不用占位图、占位数字或「待补充」假装数据完整；缺失就写缺失。

---

## 5. 命名与目录

```text
{产品}_{YYYYMMDD_HHMMSS}_{stage}.{ext}        # 时间戳为 UTC
```

| 文件 | 产出者 |
| --- | --- |
| `data/{产品}_{ts}_facts.json` | analyze.js |
| `reports/{产品}_{ts}_facts.md` | analyze.js（事实摘要，非报告） |
| `data/{产品}_{ts}_trend_context.json` | Agent（趋势检索，可选但受 3.2 触发条件约束） |
| `data/{产品}_{ts}_chart_notes.json` | Agent（附图洞察文案，可选） |
| `reports/{产品}_{ts}_report.md` | Agent（调研报告正文） |
| `reports/{产品}_{ts}_report.html` | gen-html.js（**交付物**） |
| `reports/latest_facts.md` / `latest_report.html` | 脚本复制的「最新」副本 |

```text
amz-competitor-research/
├── SKILL.md
├── _meta.json
├── scripts/
│   ├── analyze.js        # CSV -> facts.json + facts.md
│   └── gen-html.js       # report.md (+json) -> report.html
├── data/                 # *_facts.json, *_trend_context.json
└── reports/              # *_facts.md, *_report.md, *_report.html
```

---
